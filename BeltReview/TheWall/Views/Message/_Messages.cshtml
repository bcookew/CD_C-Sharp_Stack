@model List<Message>

@{
    if(Model.Count > 0)
    {
        <div class="col">
            @foreach (Message message in Model)
            {
                <h3>@message.Author.FirstName @message.Author.LastName - @message.CreatedAt.ToLongDateString()</h3>
                <p class="pl-3">@message.Text</p>
            }
        </div>
    }
    else
    {
        <div class="col text-center">
            <h2>No Messaqges to Show</h2>
        </div>
    }
}
